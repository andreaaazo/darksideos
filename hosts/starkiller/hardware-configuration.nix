# Hardware-specific configuration for starkiller.
# After first boot, copy here output of:
# `sudo nixos-generate-config --no-filesystems --dir /tmp/hw`
# Override all this file with content of /tmp/hw/hardware-configuration.nix
{ ... }:
{
  # Override everything
}
